import type { Metadata } from "next";
import "./globals.css";
import { Header } from "@/components/Header";
import { Footer } from "@/components/Footer";

export const metadata: Metadata = {
  title: "Cape Breton Realty — Properties across Cape Breton & Northeastern Nova Scotia",
  description:
    "Browse listings, parcel maps, and detailed property records across Cape Breton Island and northeastern Nova Scotia. Powered by NSAR, GeoNova, and PVSC."
};

export default function RootLayout({
  children
}: {
  children: React.ReactNode;
}) {
  return (
    <html lang="en">
      <body className="min-h-screen flex flex-col">
        <Header />
        <main className="flex-1">{children}</main>
        <Footer />
      </body>
    </html>
  );
}
